{
  "name": "basketiq-root",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd .. && mkdir -p public && cp -r client/dist/* public/"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.6.2",
    "razorpay": "^2.9.6",
    "socket.io": "^4.8.3"
  }
}
